function activeFormContainer() {
  let form = document.getElementById("formC"); // Correct the typo in the ID here
  // Toggle the "activeContainer" class
  form.classList.toggle("activeContainer");
}
